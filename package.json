{
  "name": "ai-mentor",
  "private": true,
  "workspaces": [
    "apps/web",
    "apps/api"
  ],
  "scripts": {
    "dev": "concurrently -n web,api -c cyan,magenta \"npm:dev --workspace apps/web\" \"npm:dev --workspace apps/api\"",
    "lint": "npm run lint --workspace apps/web && npm run lint --workspace apps/api",
    "build": "npm run build --workspace apps/web && npm run build --workspace apps/api",
    "start": "npm run start --workspace apps/web && npm run start --workspace apps/api"
  },
  "devDependencies": {
    "concurrently": "^9.0.0",
    "supabase": "^2.91.2"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.103.3",
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "express-session": "^1.19.0",
    "passport": "^0.7.0",
    "passport-google-oauth20": "^2.0.0"
  }
}
